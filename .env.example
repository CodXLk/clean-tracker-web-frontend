# Spring Boot backend (server-side only — never exposed to browser)
SPRING_BOOT_API_URL=http://localhost:8080
SPRING_BOOT_API_KEY=primeway-internal-dev-key

# Auth cookie signing/reference secret (min 32 chars)
AUTH_SECRET=primeway-dev-auth-secret-change-me-please-32

# Public (safe for browser)
NEXT_PUBLIC_APP_NAME=Primeway Cleaning
NEXT_PUBLIC_APP_URL=http://localhost:3000
